type Query {
  hello: String
  product(id: ID!): product
  products: [product!]!
}

type product {
  id: ID!
  name: String!
  price: Float!
  description: String!
  image: String!
  createdAt: String!
}